# 05. 문자열과 표준 라이브러리

# 문제 1. 글자 배열의 공백 바꾸기
# 다음 문자 리스트를 전달받아 이어 붙여 출력하는 print_char_list(chars) 함수와,
# 리스트 안의 공백 ' '을 쉼표 ','로 바꾸는 replace_space(chars) 함수를 작성하세요.
#
# chars = ['I', ' ', 'a', 'm', ' ', 'a', ' ', 'b', 'o', 'y']
#
# replace_space()는 원본 리스트를 직접 바꾸지 않고 새로운 리스트를 반환하도록 만드세요.
#
# 변경 전: I am a boy
# 변경 후: I,am,a,boy


# 문제 2. 문장 속 단어 수
# 한 줄짜리 문장을 반복해서 입력받아 공백으로 구분된 단어 수를 출력하세요.
# 정확히 그만을 입력하면 프로그램을 종료합니다.
# 연속된 공백이 있어도 정확히 셀 수 있도록 split()을 사용하세요.
#
# >> I love Python.
# 단어 수는 3
# >> 그만
# 프로그램 종료!


# 문제 3. 문자열 수정 명령
# 처음에 문장 하나를 입력받습니다. 이후 "찾을문자열!바꿀문자열" 형식의 명령을 반복해서 받아
# 처음 발견한 부분만 변경하고 수정된 문장을 출력하세요.
# - 명령이 정확히 두 부분으로 나뉘지 않거나 어느 한쪽이 비어 있으면 "잘못된 명령입니다!" 출력
# - 찾을 문자열이 없으면 "찾을 수 없습니다!" 출력
# - 그만을 입력하면 종료
# - 같은 문자열이 여러 번 나타나면 첫 번째 것만 변경
# str.replace(old, new, 1)을 사용한 풀이와 문자열 슬라이싱을 사용한 풀이를 작성해 보세요.
#
# >> 우리는 love Python Programming.
# 명령: 우리는!We
# We love Python Programming.
# 명령: LOV!사랑
# 찾을 수 없습니다!
# 명령: !Python
# 잘못된 명령입니다!
# 명령: love!LOVE
# We LOVE Python Programming.
# 명령: 그만
# 종료합니다.


# 문제 4. 같은 좌표의 점
# x, y 좌표를 갖는 Point 클래스를 작성하세요.
# 객체를 출력하면 "Point(3, 50)" 형식으로 보이도록 __str__()을 구현하고,
# 두 점의 좌표가 모두 같을 때만 ==의 결과가 참이 되도록 __eq__()을 구현하세요.
#
# p = Point(3, 50)
# q = Point(4, 50)
# print(p)
# print('같은 점' if p == q else '다른 점')
#
# Point(3, 50)
# 다른 점


# 문제 5. 중심이 같은 원
# 중심 좌표 x, y와 반지름 radius를 갖는 Circle 클래스를 작성하세요.
# 객체를 문자열로 표현하면 "Circle(2, 3), 반지름 5"와 같이 보이게 합니다.
# 두 원은 반지름과 관계없이 중심 좌표가 같으면 ==의 결과가 참이 되도록 구현하세요.
#
# 원 a: Circle(2, 3), 반지름 5
# 원 b: Circle(2, 3), 반지름 30
# 같은 원
